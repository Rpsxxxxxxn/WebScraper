<!DOCTYPE html>
<html lang="ja">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>スクレイピング結果 - ターゲットID <%= targetId %>
    </title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/5.0.0-beta1/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/styles.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>

<body>
    <div class="container">
        <h1 class="text-center">スクレイピング結果 - ターゲットID <%= targetId %>
        </h1>

        <canvas id="scrapingResultsChart"></canvas>

        <h2>使用方法</h2>
        <p>このページでは、特定のターゲットIDに対するスクレイピング結果をグラフとして表示します。</p>
    </div>

    <script>
        const ctx = document.getElementById('scrapingResultsChart').getContext('2d');
        const chart = new Chart(ctx, {
            type: 'line',
            data: {
                labels: <%= JSON.stringify(labels) %>,
                datasets: [{
                    label: 'スクレイピング結果',
                    data: <%= JSON.stringify(values) %>,
                    borderColor: 'rgba(75, 192, 192, 1)',
                    borderWidth: 1,
                    fill: false
                }]
            },
            options: {
                scales: {
                    x: {
                        type: 'time',
                        time: {
                            unit: 'day'
                        }
                    },
                    y: {
                        beginAtZero: true
                    }
                }
            }
        });
    </script>
</body>

</html>